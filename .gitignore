# Dependencias
node_modules/
**/node_modules/

# Lockfile de npm — el proyecto usa pnpm (ver back/package.json → packageManager).
package-lock.json
**/package-lock.json

# Variables de entorno
.env
.env.local
.env.*.local
**/.env
!.env.example
!**/.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code local cache
.claude/

# Build / cache
dist/
build/
.cache/
coverage/

# PDFs y archivos generados
*.pdf
!docs/**/*.pdf

# Documentación interna sensible (NO publicar en repo público).
# La carpeta docs/ es 100% para contexto privado: schemas de DB, endpoints,
# lógica de auth, patrones de seguridad, atajos para sesiones de Claude.
# JAMÁS subir nada de aquí.
docs/

# Runbooks REST Client — pueden contener creds tras la primera vez que
# se llenan los placeholders. Se commitean UNA vez con placeholders como
# referencia y luego se ignoran para evitar fugas accidentales.
back/scripts/test-ai.http
back/scripts/test-presupuestos.http

# Artefactos transitorios de pruebas
back/scripts/cookies.txt
back/scripts/call*.json
